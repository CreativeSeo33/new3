---
alwaysApply: false
---

# Components Index

## Catalog (Twig/UX + FSD)
- @component_delivery-selector_map.mdc — выбор способа доставки; root: `#delivery`
- @component_spinner_map.mdc — индикатор загрузки; root: `[data-module="spinner"]`
- @component_cart-counter_map.mdc — счетчик корзины; root: `[data-testid="cart-counter"]`

- @component_city-modal_map.mdc — выбор города; root: `#city-modal[data-testid="city-modal-root"]`

### Checkout page components
- @component_checkout_contact_form_map.mdc — контактная форма; root: `[data-testid="contact-form-section"]`
- @component_checkout_submit_button_map.mdc — кнопка отправки; root: `#place-order`
- @component_cart_items_map.mdc — список позиций корзины; root: `[data-testid="cart-items"]`
- @component_cart_item_map.mdc — позиция корзины; root: `[data-testid="cart-item"]`
- @component_cart_summary_map.mdc — итоги заказа; root: `#cart-total`

### Cart page components (Twig)
- @component_cart-table_map.mdc — таблица корзины; root: `tbody[data-testid="cart-items"]`
- @component_cart-item-row_map.mdc — строка товара; root: `tr[data-testid="cart-item"]`
- @component_delivery-methods-block_map.mdc — выбор доставки; root: `div[data-module="delivery-methods"]`
- @component_cart-summary_map.mdc — итоги заказа; якоря: `#cart-total` и др.
- @component_cart-actions_map.mdc — действия (оформление заказа)

### Правила и примечания
- Карты компонентов — обязательны для всех Twig‑компонентов и не pinned (`alwaysApply: false`). При добавлении/обновлении карты не изменяйте pinned‑набор.
- В шаблонах компонентов используйте якорь: `{# ai:component=<kebab> map=@component_<kebab>_map.mdc v=1 #}` (использование одного `ai:module` без `map` для компонентов не допускается).
- Добавляя новый компонент без карты — создайте `.cursor/rules/component_<kebab>_map.mdc` и добавьте запись в этот индекс.
- Интерактив без inline `<script>`: Stimulus с `"fetch": "lazy"` или FSD‑модуль (см. `@catalog_js_architecture.mdc`).


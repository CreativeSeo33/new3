# This file is the entry point to configure your own services.
# Files in the packages/ subdirectory configure your dependencies.

# Put parameters here that don't need to change on each machine where the app is deployed
# https://symfony.com/doc/current/best_practices.html#use-parameters-for-application-configuration
parameters:
    # Auth/JWT configuration (env-driven)
    env(JWT_ACCESS_TTL): '900'
    env(JWT_REFRESH_TTL): '2592000'
    env(AUTH_MAX_FAILED): '10'
    env(AUTH_LOCK_MINUTES): '15'
    env(AUTH_FAILURE_DELAY_MS_MIN): '120'
    env(AUTH_FAILURE_DELAY_MS_MAX): '280'
    env(APP_PEPPER): ''
    env(AUTH_DISPOSABLE_DOMAINS_PATH): ''

    env(AUTH_VERIFY_TTL): '86400' # 24h
    env(AUTH_PWD_RESET_TTL): '1800' # 30m
    env(APP_FRONTEND_BASE_URL): 'https://127.0.0.1:8001'

    app.auth.access_ttl: '%env(int:JWT_ACCESS_TTL)%'
    app.auth.refresh_ttl: '%env(int:JWT_REFRESH_TTL)%'
    app.auth.max_failed: '%env(int:AUTH_MAX_FAILED)%'
    app.auth.lock_minutes: '%env(int:AUTH_LOCK_MINUTES)%'
    app.auth.failure_delay_ms_min: '%env(int:AUTH_FAILURE_DELAY_MS_MIN)%'
    app.auth.failure_delay_ms_max: '%env(int:AUTH_FAILURE_DELAY_MS_MAX)%'
    app.auth.verify_ttl: '%env(int:AUTH_VERIFY_TTL)%'
    app.auth.pwd_reset_ttl: '%env(int:AUTH_PWD_RESET_TTL)%'
    app.frontend_base_url: '%env(string:APP_FRONTEND_BASE_URL)%'
    app.imagine.filters:
        - 'sm'
        - 'md'
        - 'md2'
        - 'xl'
    cart.require_precondition: false
    # default for env var if not provided in real environment
    env(DELIVERY_COURIER_SURCHARGE): '300'
    app.delivery.courier_surcharge: '%env(int:DELIVERY_COURIER_SURCHARGE)%'

    # Public delivery endpoints configuration (env-driven)
    env(DELIVERY_PUBLIC_TTL): '600'
    env(DELIVERY_POINTS_DEFAULT_LIMIT): '20'
    env(DELIVERY_POINTS_MAX_LIMIT): '100'
    delivery.public.ttl_seconds: '%env(int:DELIVERY_PUBLIC_TTL)%'
    delivery.points.default_limit: '%env(int:DELIVERY_POINTS_DEFAULT_LIMIT)%'
    delivery.points.max_limit: '%env(int:DELIVERY_POINTS_MAX_LIMIT)%'

    # Delivery configuration (defaults via env)
    env(DELIVERY_FREE_THRESHOLD_DEFAULT): '3000'
    env(DELIVERY_TYPES): 'pvz,courier'
    # Default sender email if not provided via real environment
    env(ADMIN_FROM_EMAIL): 'no-reply@example.com'
    app.delivery.free_threshold_default: '%env(int:DELIVERY_FREE_THRESHOLD_DEFAULT)%'
    app.delivery.types: '%env(csv:DELIVERY_TYPES)%'

    # Cart cookie configuration
    cart.cookie.force_secure_in_prod: true
    cart.cookie.use_host_prefix: true
    cart.cookie.name: 'cart_id'
    cart.cookie.ttl_days: 180
    cart.cookie.domain: null
    cart.cookie.same_site: 'lax'

    # Wishlist cookie configuration
    wishlist.cookie.force_secure_in_prod: true
    wishlist.cookie.use_host_prefix: true
    wishlist.cookie.name: 'wishlist_token'
    wishlist.cookie.ttl_days: 365
    wishlist.cookie.domain: null
    wishlist.cookie.same_site: 'lax'

    # Notification settings
    app.notification.from_email: '%env(ADMIN_FROM_EMAIL)%'

    # Search engine configuration (env-driven)
    env(SEARCH_ENGINE): 'mysql'
    env(SEARCH_TNT_STORAGE): '%kernel.project_dir%/var/search/indexes'
    env(SEARCH_TNT_AS_YOU_TYPE): '1'
    env(SEARCH_TNT_FUZZY): '1'
    env(SEARCH_TNT_FUZZY_DISTANCE): '1'
    search.engine: '%env(string:SEARCH_ENGINE)%'
    search.tnt.storage: '%env(string:SEARCH_TNT_STORAGE)%'
    search.tnt.as_you_type: '%env(bool:SEARCH_TNT_AS_YOU_TYPE)%'
    search.tnt.fuzzy: '%env(bool:SEARCH_TNT_FUZZY)%'
    search.tnt.fuzzy_distance: '%env(int:SEARCH_TNT_FUZZY_DISTANCE)%'

    # Facets configuration (env-driven)
    env(FACETS_CACHE_TTL): '90'
    env(FACET_VALUES_LIMIT): '20'
    app.facets.cache_ttl: '%env(int:FACETS_CACHE_TTL)%'
    app.facets.values_limit_default: '%env(int:FACET_VALUES_LIMIT)%'

    # Yandex Delivery API configuration (env-driven)
    env(YANDEX_DELIVERY_BASE_URL): 'https://b2b-authproxy.taxi.yandex.net'
    env(YANDEX_DELIVERY_TOKEN): ''
    app.yandex_delivery.base_url: '%env(string:YANDEX_DELIVERY_BASE_URL)%'
    app.yandex_delivery.token: '%env(string:YANDEX_DELIVERY_TOKEN)%'

    # Yandex Maps API (env-driven)
    env(YANDEX_MAPS_API_KEY): ''
    app.yandex_maps.api_key: '%env(string:YANDEX_MAPS_API_KEY)%'

    # Theme system flag default
    env(APP_THEME_ENABLED): '0'

    # Catalog pagination (category page)
    app.pagination.allowed_limits: [10, 20, 30]
    app.pagination.default_limit: 10

services:
    # default configuration for services in *this* file
    _defaults:
        autowire: true      # Automatically injects dependencies in your services.
        autoconfigure: true # Automatically registers your services as commands, event subscribers, etc.
    App\State\UserPasswordProcessor: ~


    # makes classes in src/ available to be used as services
    # this creates a service per class whose id is the fully-qualified class name
    App\:
        resource: '../src/'
        exclude: '../src/{DependencyInjection,Entity/Migrations,Tests,Kernel.php}'

    # add more service definitions when explicit configuration is needed
    # please note that last definitions always *replace* previous ones

    # Doctrine entity listener as service (attributes already present, but ensure service is public)
    App\Doctrine\Listener\ProductEntityListener: ~
    App\Doctrine\Listener\OptionEntityListener: ~

    # Cart context service
    App\Service\CartContext:
        arguments:
            - '@doctrine.orm.entity_manager'
            - '@App\Repository\CartRepository'
            - '@lock.factory'
            - '@request_stack'
            - '@App\Http\CartCookieFactory'

    # Cart cookie listener
    App\EventListener\CartCookieListener:
        tags:
            - { name: kernel.event_listener, event: kernel.response, method: onKernelResponse }

    # Cart login subscriber
    App\EventSubscriber\CartLoginSubscriber:
        arguments:
            - '@App\Repository\CartRepository'
            - '@App\Service\CartManager'
            - '@doctrine.orm.entity_manager'
            - '@App\Http\CartCookieFactory'

    # Remove Intervention Image wiring (migrated to LiipImagine)

    # Inject project dir for services that need filesystem paths
    App\Controller\Admin\MediaAdminController:
        tags: [ 'controller.service_arguments' ]
        arguments:
            $projectDir: '%kernel.project_dir%'
            $imagineFilters: '%app.imagine.filters%'

    App\Controller\ConfigController:
        tags: [ 'controller.service_arguments' ]

    App\Controller\Api\DeliveryPublicController:
        tags: ['controller.service_arguments']
        arguments:
            $pointsDefaultLimit: '%delivery.points.default_limit%'
            $pointsMaxLimit: '%delivery.points.max_limit%'
            $publicTtlSeconds: '%delivery.public.ttl_seconds%'

    App\Service\ImageCacheService:
        arguments:
            $projectDir: '%kernel.project_dir%'
            $cacheManager: '@liip_imagine.cache.manager'
            $filterManager: '@liip_imagine.filter.manager'
            $filterConfig: '@liip_imagine.filter.configuration'
            $dataManager: '@liip_imagine.data.manager'

    App\Service\ImageWarmupService: ~

    # Footer categories service
    App\Service\FooterCategoryService:
        public: true

    # Navbar categories service
    App\Service\NavbarCategoryService:
        public: true

    # Product state processor with cart item repository
    App\State\ProductStateProcessor:
        arguments:
            $cartItemRepository: '@App\Repository\CartItemRepository'

    App\State\ProductStateProvider:
        arguments:
            $searchEngine: '%search.engine%'

    # Product copy service
    App\Service\ProductCopyService:
        arguments:
            $lifecycleService: '@App\Service\ProductLifecycleService'

    # Delivery provider registry
    App\Service\Delivery\Provider\DeliveryProviderRegistry:
        arguments:
            $providers: []

    # Delivery methods auto-registration
    App\Service\Delivery\Method\PvzDeliveryMethod:
        tags: ['app.delivery_method', 'app.delivery_provider']
        bind:
            # Указываем, что в конструктор этого класса в аргумент с именем $calculationType
            # нужно передать строку 'cost_per_item'.
            # Чтобы отключить расчет по количеству, просто измените на 'flat_rate'.
            $calculationType: 'cost_per_item'
        arguments:
            $defaultFreeThreshold: '%app.delivery.free_threshold_default%'

    App\Service\Delivery\Method\CourierDeliveryMethod:
        tags: ['app.delivery_method', 'app.delivery_provider']
        bind:
            # Для курьера тоже можно задать свой тип расчета.
            $calculationType: 'cost_per_item'
        arguments:
            $surcharge: '%app.delivery.courier_surcharge%'
            $defaultFreeThreshold: '%app.delivery.free_threshold_default%'

    # Cart ETag and write guard services
    App\Http\CartEtags: ~

    App\Http\CartWriteGuard:
        arguments:
            $requirePrecondition: '%cart.require_precondition%'

    App\Http\CartResponse: ~

    # Cart delta builder for optimized responses
    App\Service\CartDeltaBuilder: ~

    # Cart cookie factory for secure cookie management
    App\Http\CartCookieFactory:
        arguments:
            $forceSecureInProd: '%cart.cookie.force_secure_in_prod%'
            $useHostPrefix: '%cart.cookie.use_host_prefix%'
            $cookieName: '%cart.cookie.name%'
            $ttlDays: '%cart.cookie.ttl_days%'
            $domain: '%cart.cookie.domain%'
            $sameSite: '@=constant("Symfony\\Component\\HttpFoundation\\Cookie::SAMESITE_LAX")'

    # Wishlist services
    App\Http\WishlistCookieFactory:
        arguments:
            $forceSecureInProd: '%wishlist.cookie.force_secure_in_prod%'
            $useHostPrefix: '%wishlist.cookie.use_host_prefix%'
            $cookieName: '%wishlist.cookie.name%'
            $ttlDays: '%wishlist.cookie.ttl_days%'
            $domain: '%wishlist.cookie.domain%'
            $sameSite: '%wishlist.cookie.same_site%'

    App\Service\WishlistContext:
        arguments:
            $cookieName: '@=service("App\\Http\\WishlistCookieFactory").getCookieName()'

    App\EventSubscriber\WishlistLoginSubscriber:
        arguments:
            - '@App\Repository\WishlistRepository'
            - '@App\Service\WishlistManager'
            - '@doctrine.orm.entity_manager'
            - '@App\Http\WishlistCookieFactory'

    App\Twig\WishlistExtension:
        tags: [ 'twig.extension' ]

    # Idempotency services for Cart API
    App\Service\Idempotency\IdempotencyService: ~
    App\Service\Idempotency\IdempotencyRequestHasher: ~

    # Idempotency logger channel
    monolog.logger.idempotency:
        class: Psr\Log\LoggerInterface
        factory: ['@monolog.logger', 'getLogger']
        arguments:
            - idempotency

    # Yandex Delivery API client
    App\Service\Yandex\YandexDeliveryApi:
        arguments:
            $baseUrl: '%app.yandex_delivery.base_url%'
            $token: '%app.yandex_delivery.token%'
            $timeoutSeconds: 15

    # Facets services
    App\Service\FacetIndexer: ~

    App\EventSubscriber\FacetReindexSubscriber:
        tags:
            - { name: kernel.event_subscriber }

    App\Doctrine\Subscriber\FacetDoctrineSubscriber:
        tags:
            - { name: doctrine.event_subscriber }

    App\Doctrine\Subscriber\ProductSearchIndexerSubscriber:
        tags:
            - { name: doctrine.event_subscriber }

    App\Controller\Admin\FacetController:
        tags: ['controller.service_arguments']
        arguments:
            $cacheTtl: '%app.facets.cache_ttl%'

    App\Controller\Admin\FacetConfigController:
        tags: ['controller.service_arguments']
        arguments:
            $defaultValuesLimit: '%app.facets.values_limit_default%'

    App\Controller\Catalog\FacetsController:
        tags: ['controller.service_arguments']
        arguments:
            $publicTtl: '%app.facets.cache_ttl%'
            $defaultValuesLimit: '%app.facets.values_limit_default%'

    App\OpenApi\ProductOpenApiDecorator:
        decorates: 'api_platform.openapi.factory'
        arguments:
            - '@App\OpenApi\ProductOpenApiDecorator.inner'

    # Search services
    App\Service\Search\TNTSearchFactory:
        arguments:
            $storagePath: '%search.tnt.storage%'
            $asYouType: '%search.tnt.as_you_type%'
            $fuzzy: '%search.tnt.fuzzy%'
            $fuzzyDistance: '%search.tnt.fuzzy_distance%'

    App\Service\Search\RuQueryNormalizer:
        arguments:
            $stopWords: []

    App\Service\Search\ProductIndexer: ~

    App\Service\Search\ProductSearch: ~

    # Theme System
    App\Theme\ThemeManager:
        arguments:
            $themesPath: '%kernel.project_dir%/themes'
            $defaultTheme: '%app_theme.default%'
            $cacheEnabled: '%app_theme.cache_enabled%'
            $cacheTtl: '%app_theme.cache_ttl%'
            $allowedThemes: '%app_theme.allowed_themes%'

    App\Theme\EventListener\ThemeListener:
        arguments:
            $defaultTheme: '%app_theme.default%'
            $allowedThemes: '%app_theme.allowed_themes%'
            $enabled: '%app_theme.enabled%'
        tags:
            - { name: kernel.event_listener, event: kernel.request, priority: 64 }

    App\Theme\Twig\ThemeLoader:
        arguments:
            $themes: '@App\Theme\ThemeManager'
            $enabled: '%app_theme.enabled%'
        tags: ['twig.loader']

    App\Theme\Twig\ThemeExtension:
        tags: ['twig.extension']

    App\Theme\Asset\ThemeAssetPackage:
        arguments:
            $projectDir: '%kernel.project_dir%'
            $enabled: '%app_theme.enabled%'
        public: true

    # Prefer cookie-based token over Authorization header for JWT extraction
    # Decorate Lexik chain extractor with cookie-first behavior
    App\Security\Jwt\CookieFirstChainExtractor:
        decorates: 'lexik_jwt_authentication.extractor.chain_extractor'
        arguments:
            $cookieExtractor: '@lexik_jwt_authentication.extractor.cookie_extractor'
            $splitCookieExtractor: '@lexik_jwt_authentication.extractor.split_cookie_extractor'
            $inner: '@App\Security\Jwt\CookieFirstChainExtractor.inner'
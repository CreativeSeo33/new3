{% extends 'catalog/base.html.twig' %}

{% block title %}Оформление заказа — {{ parent() }}{% endblock %}

{% block catalog_body %}
{# ai:page=checkout map=@page_checkout_map.mdc v=1 #}
  <div class="container mx-auto px-4 py-6" data-module="checkout-form" data-submit-url="{{ path('checkout_submit') }}" data-testid="checkout-root">
    <h1 class="text-2xl font-semibold mb-6">Оформление заказа</h1>

    <div class="relative grid grid-cols-1 md:grid-cols-3 gap-6">
      <div class="md:col-span-2 space-y-6">
        {% include 'components/checkout/contact-form.html.twig' %}
        
        {% include 'catalog/_delivery_selector.html.twig' ignore missing %}
        

        {# Блок оплаты удалён по требованию #}
      </div>

      <aside class="md:col-span-1 p-4 border rounded h-fit">
        <h2 class="text-lg font-medium mb-4">Ваш заказ</h2>
        {% if cart.items|length > 0 %}
          {% include 'components/cart/items.html.twig' with { items: cart.items } %}
          {% include 'components/cart/summary.html.twig' with { cart: cart, delivery: delivery } %}
        {% else %}
          <div class="text-gray-500 text-sm">Корзина пуста</div>
        {% endif %}
        {% include 'components/checkout/submit-button.html.twig' with { disabled: cart.items|length == 0 } %}
      </aside>

      {# Overlay spinner over the whole grid #}
      {% include 'components/spinner.html.twig' with {
        id: 'checkout-spinner',
        overlay: true,
        visible: false,
        size: 'large',
        extra_class: ''
      } %}
    </div>

    <p class="text-xs text-gray-500 mt-6">Нажимая кнопку, вы соглашаетесь с условиями оферты и политикой конфиденциальности.</p>
  </div>

  {{ component('CityModal') }}

  {# inline script removed: migrated to FSD feature checkout-form #}
{% endblock %}

{% block catalog_footer %}{% endblock %}

{% block catalog_navbar %}{% endblock %}

{% block catalog_header %}
<header class="bg-white shadow-sm">
  <div class="py-4 border-b border-gray-100">
    <div class="container mx-auto px-4">
      <div class="flex items-center justify-between">
        <div class="flex items-center">
          <a href="{{ path('catalog_index') }}" class="flex items-center gap-3">
            {% if logo_url is defined and logo_url %}
              <img src="{{ logo_url }}" alt="{{ site_name|default('Shop') }}" class="h-12 w-auto" />
            {% else %}
              <div class="w-12 h-12 bg-blue-600 rounded-lg flex items-center justify-center">
                <span class="text-white font-bold text-xl">L</span>
              </div>
            {% endif %}
            <span class="text-2xl font-bold text-gray-800 hidden sm:block">{{ site_name|default('ShopGrids') }}</span>
          </a>
        </div>
      </div>
    </div>
  </div>
</header>
{% endblock %}




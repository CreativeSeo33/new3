<!DOCTYPE html>
<html lang="ru">
  <head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>{% block title %}Symfony + Vue{% endblock %}</title>
    <meta name="csrf-token" content="{{ csrf_token('api') }}">
    {# app entry removed #}
    {% block styles %}{% endblock %}
  </head>
  <body class="antialiased">
    {% block body %}{% endblock %}

    {# app entry removed #}
    {% block javascripts %}{% endblock %}
  </body>
  
</html>

{# Удалён второй дублирующийся HTML-документ из рецепта Symfony #}
